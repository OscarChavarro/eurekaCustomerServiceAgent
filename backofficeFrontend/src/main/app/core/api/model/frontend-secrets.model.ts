export interface FrontendSecrets {
  backend: {
    baseUrl: string;
  };
  contactsBackend: {
    baseUrl: string;
  };
  retrievalBackend: {
    baseUrl: string;
  };
  staticAssets: {
    baseUrl: string;
  };
}
