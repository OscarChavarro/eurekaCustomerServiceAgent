export interface FrontendSecrets {
  backend: {
    baseUrl: string;
  };
  contacts: {
    prospectPreffix: string;
  };
  contactsBackend: {
    baseUrl: string;
  };
  retrievalBackend: {
    baseUrl: string;
  };
  whatsappConnectorBackend: {
    baseUrl: string;
  };
  staticAssets: {
    baseUrl: string;
  };
}
